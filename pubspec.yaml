name: app
description: Money Note App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  uuid: ^4.5.1
  table_calendar: ^3.0.9
  flutter_slidable: ^3.1.1
  fl_chart: ^0.68.0
  intl: ^0.19.0
  provider: ^6.1.5+1
  firebase_auth: ^5.7.0
  iconify_flutter: ^0.0.7
  shared_preferences: ^2.5.5
  google_generative_ai: ^0.4.7
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env